import { CalendarUTCDirective } from './calendar-utc.directive';

describe('CalendarUTCDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarUTCDirective();
    expect(directive).toBeTruthy();
  });
});
